using UnityEngine;

public class BottleCapMovement : MonoBehaviour
{
    [SerializeField] Transform targetPosition;
    

    // Update is called once per frame
    void Update()
    {
        
    }
}
